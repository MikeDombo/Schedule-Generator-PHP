doctype html
html(lang="en")
    head
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no")
        meta(name="description" content="Schedule Generator for students.")
        meta(name="og:title" content="University Schedule Generator")
        meta(name="og:description" content="Custom college schedule generator for students.")
        title #{title}
        link(rel="stylesheet" href="public/css/bootstrap.min.css")
        script(type="text/javascript" src="public/js/jquery.min.js")
        script(type="text/javascript" src="public/js/bootstrap.min.js")
        script(src="https://cdn.ravenjs.com/3.26.4/raven.min.js" crossorigin="anonymous" type="text/javascript")
        script(async src="https://www.googletagmanager.com/gtag/js?id=UA-4436865-11" type="text/javascript")
        script(type="text/javascript").
            if (location.hostname !== "localhost" && location.hostname !== "127.0.0.1") {
                window.dataLayer = window.dataLayer || [];
                function gtag () {
                    dataLayer.push(arguments);
                }
                gtag('js', new Date());
                gtag('config', 'UA-4436865-11');
                (function () {
                    var t, i, e, n = window, o = document, a = arguments, s = "script",
                        r = ["config", "track", "identify", "visit", "push", "call", "trackForm", "trackClick"],
                        c = function () {
                            var t, i = this;
                            for (i._e = [], t = 0; r.length > t; t++) (function (t) {
                                i[t] = function () {
                                    return i._e.push([t].concat(Array.prototype.slice.call(arguments, 0))), i
                                }
                            })(r[t])
                        };
                    for (n._w = n._w || {}, t = 0; a.length > t; t++) n._w[a[t]] = n[a[t]] = n[a[t]] || new c;
                    i = o.createElement(s), i.async = 1, i.src = "//static.woopra.com/js/w.js", e = o.getElementsByTagName(s)[0], e.parentNode.insertBefore(i, e)
                })("woopra");
                woopra.config({
                    domain: 'mikedombrowski.com'
                });
                woopra.track();
            }
            Raven.config('https://f23d61188d4845e3aaddc9be1bd4ec96@sentry.home.mikedombrowski.com/4').install()
        block after_head
    body
        block nav
        block content
    block after_body
